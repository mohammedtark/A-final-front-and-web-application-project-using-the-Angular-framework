import { FilterproductsnotfeaturedPipe } from './filterproductsnotfeatured.pipe';

describe('FilterproductsnotfeaturedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterproductsnotfeaturedPipe();
    expect(pipe).toBeTruthy();
  });
});
